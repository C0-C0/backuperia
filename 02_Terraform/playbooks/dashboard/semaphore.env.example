# Copy to /etc/semaphore/semaphore.env, fill in, chmod 600. Keep OUT of git.
# Loaded by the systemd unit; inventory.yml reads PROXMOX_TOKEN_SECRET via
# lookup('env', 'PROXMOX_TOKEN_SECRET') so the token never lands in the repo.
PROXMOX_TOKEN_SECRET=your-proxmox-api-token-secret-uuid
